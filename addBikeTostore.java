/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/GUIForms/JFrame.java to edit this template
 */
package view;

import controller.BikeDAO;
import java.awt.BorderLayout;
import java.awt.GridLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.io.BufferedWriter;
import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.io.PrintWriter;
import javax.swing.JButton;
import javax.swing.JCheckBox;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JTextField;
import model.Bike;

/**
 *
 * @author dominick valentine
 */
public class addBikeTostore extends javax.swing.JFrame {
private JTextField typeField;
    private JTextField gearsField;
    private JTextField costField;
   
    private JTextField tireSizeField;
    private JButton addButton;
   private JButton menubutton;
    
    // exit 
    private JButton endButton;
    
    // bike 
    private BikeDAO bDAO;
 //   private addBikeTostore bb;
    /**
     * Creates new form BikeStore
     */
    public addBikeTostore() {
         super("Bike GUI");
        setLayout(new BorderLayout());
         // Create the input fields and labels
        JPanel inputPanel = new JPanel(new GridLayout(6, 2));
        inputPanel.add(new JLabel("Bike type:"));
        inputPanel.add(typeField = new JTextField());
        inputPanel.add(new JLabel("Number of gears:"));
        inputPanel.add(gearsField = new JTextField());
        inputPanel.add(new JLabel("Cost:"));
        inputPanel.add(costField = new JTextField());
       
        inputPanel.add(new JLabel("Tire size:"));
        inputPanel.add(tireSizeField = new JTextField());
        //
        

        add(inputPanel, BorderLayout.NORTH);

        // Create the add button
        addButton = new JButton("Add");
        addButton.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
                //
                addBike();
              // bDAO.createBike();
                
            }
        });
        
        add(addButton, BorderLayout.SOUTH);
       
        
 // end 
  endButton= new JButton("Exit");
   //endButton.setBounds(3,5,20,20);
  endButton.addActionListener(new ActionListener() {
        @Override
        public void actionPerformed(ActionEvent e) {
               exit();
            }
       });
        add(endButton,BorderLayout.EAST);
       // back to start
       
        menubutton = new JButton("Menu");
        menubutton.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
              Menu abt = new Menu();
              abt.setLocationRelativeTo(null);
           
              abt.show();
              
              dispose();// close current frame after other one opens
                
                //addBike();
            }
        });
        
        add(menubutton,BorderLayout.WEST);
        
    

        // Set the window size and make it visible
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);
    }
    
    private void exit(){
  System.exit(0);
 
 }
       // addinn bikes
    // put the input into the bike ojects
    // and wrtie this to the bike file 
    private void addBike() {
       // taking user input and putting to a bike
        String type = typeField.getText();
        int gears = Integer.parseInt(gearsField.getText());
        double cost = Double.parseDouble(costField.getText());
        double tireSize = Double.parseDouble(tireSizeField.getText());
        bDAO = new BikeDAO();
        bDAO.createBike(type,gears,cost,tireSize);
     
    
         
    }
    
   

    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setDefaultCloseOperation(javax.swing.WindowConstants.EXIT_ON_CLOSE);

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(getContentPane());
        getContentPane().setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 400, Short.MAX_VALUE)
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 300, Short.MAX_VALUE)
        );

        pack();
    }// </editor-fold>//GEN-END:initComponents

    /**
     * @param args the command line arguments
     */
    

    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
